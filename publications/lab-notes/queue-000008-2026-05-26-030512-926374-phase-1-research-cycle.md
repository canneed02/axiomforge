# Phase 1 Research Cycle

This note was generated by the AxiomForge autonomous research system.

Claim type: `measured`

Evidence:

```text
task_id=11; run_id=5; proposal=/root/axiomforge-state/artifacts/research-runs/2026-05-26T030408.122142Z0000-maintain-autonomous-research-memory/proposal.json; verifier=/root/axiomforge-state/artifacts/research-runs/2026-05-26T030408.122142Z0000-maintain-autonomous-research-memory/verifier.json
```

## Observation

The autonomous system created a Phase 1 research cycle for `maintain autonomous research memory`. It selected
the `methodology` program, wrote machine-readable proposal and
verification artifacts, and checked that provider secrets were redacted from
public artifacts.

External provider memo:

```text
**Research Memo: Autonomous Research Memory**

---

## 1. Falsifiable Claim

A retrieval-augmented generation (RAG) system with **episodic memory consolidation**—where intermediate research steps are compressed via gradient-based summarization and stored in a structured knowledge graph with temporal decay—outperforms naive append-only context baselines on long-horizon scientific task sequences (>50 steps) by ≥15% on answer accuracy, while using ≤30% of the token budget.

---

## 2. Experiment / Proof Harness

**HARNESS-MEM-1**

| Component | Specification |
|-----------|-------------|
| **Benchmark** | Custom suite: 200 synthetic multi-step scientific reasoning chains (e.g., "design PCR primers → check off-targets → order → troubleshoot gel") derived from Bio-protocol and Methods in Enzymology |
| **Baselines** | (a) Naive full-context append; (b) Fixed-size sliding window; (c) Oracle human summary |
| **Treatment** | Episodic RAG with: (i) gradient saliency for step compression, (ii) knowledge graph (Neo4j) with TTL decay, (iii) retrieval via dense + sparse hybrid |
| **Metrics** | Task success rate; hallucination rate (LLM-judge + human audit); cumulative tokens; retrieval precision@k |
| **Infrastructure** | Reproducible via Docker; all prompts, graph schemas, and evaluation scripts versioned |

---

## 3. Failure Modes

| ID | Failure Mode | Detection |
|----|-------------|-----------|
| **F1** | **Catastrophic condensation**: critical negative results (e.g., "X failed, do not retry") are compressed out, causing loops | Audit trail of compressed-out nodes; inject adversarial "poison pill" tasks |
| **F2** | **Retrieval collapse**: embedding drift causes semantically similar but procedurally distinct tasks to retrieve wrong precedents | Track retrieval confidence variance; measure cross-task interference |
| **F3** | **Graph topology ossification**: frequent paths get over-reinforced, preventing exploration of novel solution branches | Monitor edge weight entropy; detect "rich get richer" dynamics |

---

## 4. Minimal Publishable Artifact

**`memory-condensation-v0.1.tar.gz`** — a self-contained, executable package containing:
- 50-line Python reference implementation of the gradient-saliency compressor
- JSON schema for the knowledge graph node/edge types
- `repro.sh` that: installs deps, runs on 5 toy examples, outputs metrics CSV
- Signed SHA-256 manifest; hosted on Zenodo with DOI

No claims of superiority to all possible systems; only that the artifact instantiates the claim and enables independent falsification.

---
```

## Publication Queue

The note is queued as a release candidate, not silently pushed as a finished
scientific result. Public release requires policy, hygiene, and reproducibility
checks.

## Limitations

This autonomous cycle validates the operating loop. It does not claim that an
open scientific problem is solved. Provider output, when present, is treated as
a proposal input rather than evidence of truth.

