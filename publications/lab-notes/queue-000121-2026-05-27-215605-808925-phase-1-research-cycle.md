# Phase 1 Research Cycle

This note was generated by the AxiomForge autonomous research system.

Claim type: `measured`

Evidence:

```text
task_id=124; run_id=46; proposal=/root/axiomforge-state/artifacts/research-runs/2026-05-27T215436.986646Z0000-maintain-autonomous-research-memory/proposal.json; verifier=/root/axiomforge-state/artifacts/research-runs/2026-05-27T215436.986646Z0000-maintain-autonomous-research-memory/verifier.json
```

## Observation

The autonomous system created a Phase 1 research cycle for `maintain autonomous research memory`. It selected
the `methodology` program, wrote machine-readable proposal and
verification artifacts, and checked that provider secrets were redacted from
public artifacts.

External provider memo:

```text
**Research Memo: Autonomous Research Memory**

---

## 1. Falsifiable Claim

A retrieval-augmented generation (RAG) system with explicit provenance tracking and confidence calibration reduces hallucinated citations in autonomous scientific literature review by ≥40% compared to standard vector-retrieval baselines, measured by human-verified citation accuracy on a held-out set of 500 papers from arXiv CS 2023-2024.

---

## 2. Experiment / Proof Harness

**System:** `research-memory-v0` — a modular harness with:
- **Retrieval:** Hybrid dense-sparse index (ColBERTv2 + BM25) over arXiv abstracts
- **Provenance:** Every generated claim tagged with `(paper_id, chunk_id, retrieval_score)`
- **Calibration:** Temperature-scaled confidence scores trained on historical retrieval precision

**Evaluation Protocol:**
- Generate 1,000 literature-review paragraphs (synthetic queries from paper titles)
- Human annotators verify: (a) citation exists, (b) supports claim, (c) no cherry-picking
- Primary metric: `verified_citation_rate` — fraction of claims with correct, non-misrepresented citations

**Baseline:** Standard top-k cosine similarity with GPT-4 generation.

---

## 3. Failure Modes

| ID | Failure Mode | Detection |
|---|---|---|
| **F1** | *Retrieval collapse*: High-confidence retrieval from review papers masquerading as primary sources | Check `paper_type` metadata; flag secondary-source chains |
| **F2** | *Confidence miscalibration under distribution shift*: System overconfident on out-of-domain queries (e.g., biology papers in CS index) | Track domain classifier entropy; reject if above threshold |
| **F3** | *Provenance laundering*: Model paraphrases retrieved text sufficiently to invert meaning, while preserving citation | Semantic entailment check between source chunk and generated claim; flag non-entailment |

---

## 4. Minimal Publishable Artifact

**The `research-memory-v0` benchmark dataset:** 500 arXiv papers with human-verified claim-citation pairs, retrieval traces, and calibrated confidence scores — released with reproducible evaluation scripts (Dockerized, deterministic seeding). Enables transparent comparison of future systems on citation fidelity rather than fluency.

---

*Status: Preliminary. Baseline experiments running. Seeking replication before claim assertion.*
```

## Publication Queue

The note is queued as a release candidate, not silently pushed as a finished
scientific result. Public release requires policy, hygiene, and reproducibility
checks.

## Limitations

This autonomous cycle validates the operating loop. It does not claim that an
open scientific problem is solved. Provider output, when present, is treated as
a proposal input rather than evidence of truth.

