# Phase 1 Research Cycle

This note was generated by the AxiomForge autonomous research system.

Claim type: `measured`

Evidence:

```text
task_id=74; run_id=27; proposal=/root/axiomforge-state/artifacts/research-runs/2026-05-27T020342.001951Z0000-maintain-autonomous-research-memory/proposal.json; verifier=/root/axiomforge-state/artifacts/research-runs/2026-05-27T020342.001951Z0000-maintain-autonomous-research-memory/verifier.json
```

## Observation

The autonomous system created a Phase 1 research cycle for `maintain autonomous research memory`. It selected
the `methodology` program, wrote machine-readable proposal and
verification artifacts, and checked that provider secrets were redacted from
public artifacts.

External provider memo:

```text
**Research Memo: Autonomous Research Memory**

---

## 1. Falsifiable Claim

A retrieval-augmented generation (RAG) system with explicit provenance tracking and confidence calibration reduces hallucinated citations in autonomous scientific literature review by ≥40% compared to standard vector-retrieval baselines, measured by human-verified citation accuracy on a held-out set of 500 papers from arXiv CS 2023-2024.

---

## 2. Experiment / Proof Harness

**System:** `research-memory-v0` — a modular harness with:
- **Retrieval:** Hybrid dense-sparse index (colbertv2 + BM25) over arXiv abstracts
- **Provenance:** Every claim linked to source paragraph with exact span; confidence = min(retrieval score, generation entailment score from NLI model)
- **Calibration:** Temperature-scaled confidence bins; reject claims below threshold

**Evaluation Protocol:**
- Generate 1000 literature-review paragraphs per condition
- Human annotators verify: (a) citation exists, (b) cited text supports claim, (c) no fabricated context
- Primary metric: `verified_citation_rate` (VCR)

**Code & Data:** GitHub repo with deterministic seeding, Docker environment, and evaluation scripts.

---

## 3. Failure Modes

| Mode | Description | Detection |
|------|-------------|-----------|
| **Retrieval Collapse** | High similarity to incorrect corpus region due to abstract superficiality; e.g., "transformer" matches methodology but not claim intent | Track query-to-chunk semantic drift via KL divergence between query and retrieved embeddings |
| **Confidence Overcalibration** | Model assigns high confidence to synthesized "facts" that merge multiple sources; NLI score masks contradiction | Adversarial test: inject contradictory source; measure if confidence drops |
| **Temporal Drift** | System retrieves outdated preprints that contradict later published versions; no versioning in index | Maintain paper version graph; flag when retrieved chunks have newer contradicting versions |

---

## 4. Minimal Publishable Artifact

**"research-memory-v0: A Reproducible Harness for Citation-Verified Scientific RAG"** — a GitHub repository containing:
- Complete retrieval + generation pipeline (≤2000 lines Python)
- Annotated evaluation dataset of 500 papers with human verification labels
- `RESULTS.md`: VCR scores with confidence-calibration curves, per-failure-mode error analysis, and ablation removing provenance tracking

Published under OSI-approved license with pinned dependencies and GitHub Actions CI for reproducibility.

---

*Status: Preliminary architecture complete; human annotation in progress. Not claimed solved.*
```

## Publication Queue

The note is queued as a release candidate, not silently pushed as a finished
scientific result. Public release requires policy, hygiene, and reproducibility
checks.

## Limitations

This autonomous cycle validates the operating loop. It does not claim that an
open scientific problem is solved. Provider output, when present, is treated as
a proposal input rather than evidence of truth.

