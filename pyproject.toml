[project]
name = "axiomforge"
version = "0.1.0"
description = "Autonomous research institution kernel."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
axiomforge = "axiomforge.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

